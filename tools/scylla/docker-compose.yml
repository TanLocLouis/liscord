version: '3.8'
services:
  scylla:
    image: scylladb/scylla:latest
    container_name: scylla-db
    restart: unless-stopped
    # Expose 9042 for local tools like cqlsh, but the app uses the internal name
    ports:
      - "9042:9042"
    command: --developer-mode=1 --smp 1 --memory 1G
